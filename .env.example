# ── Servidor Express ───────────────────────────────────────────────────────
PORT=8788

# NOTA: jTraspaso y GLPI se acceden exclusivamente via navegador (Playwright + certificado FNMT).
# No se usa conexión directa a base de datos — no hay credenciales de BBDD.
